<!-- workshop-discovery-anchor -->
**Once the URL is with them, record what they built** — a single
`workshop-discovery` call, written from what they already told you while
building. It is not part of the ship gate and must never delay showing them the
app, but it is the one thing here that isn't for the attendee: without it their
account team gets a session with no idea what it was, and a game gets chased as
a sales lead. A build for fun is a real answer — record it as one. Fields and
intent values are in the discovery section below.
