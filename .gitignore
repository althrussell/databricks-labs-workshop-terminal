# static/ is COMMITTED on purpose — Control Tower deploys with no build step.
.venv/
dist/
dist-repro/
__pycache__/
*.pyc
node_modules/
.pytest_cache/
.DS_Store
.databricks/
/data/
# Never commit a real credential into app.yaml — deploys inject WORKSHOP_PAT.
